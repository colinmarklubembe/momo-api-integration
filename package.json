{
  "name": "momo-api-integration",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start:dev": "bun run src/index.ts",
    "test": "bun test",
    "dev": "bun --watch src/index.ts --hot",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/src/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/express": "^5.0.2",
    "axios": "^1.9.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "uuid": "^11.1.0"
  }
}
